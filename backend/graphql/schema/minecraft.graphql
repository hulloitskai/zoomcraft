scalar Coordinates
scalar Orientation

type Player {
  username: String!
  position: Coordinates!
  orientation: Orientation!
}

extend type Query {
  players: [Player]!
  player(username: String!): Player
}
